package de.renew.usecasecomponents.templates.$smallAppName;

import de.renew.agent.repr.common.KVT;
import de.renew.agent.repr.common.KVTFactory;
import de.renew.agent.repr.common.VT;
import de.renew.agent.repr.common.VTFactory;

import de.renew.plugin.PluginAdapter;
import de.renew.plugin.PluginException;
import de.renew.plugin.PluginProperties;

import de.renew.util.ClassSource;

import java.net.URL;


/**
 * This is a generated file. Generated by PluginGenerator
 * Representative for the $appName plug-in.
 *
 * @author PluginGenerator
 */
public class ${appName}Plugin extends PluginAdapter {
    public ${appName}Plugin(URL url) throws PluginException {
        super(url);
        registerClasses();
    }

    public ${appName}Plugin(PluginProperties props) {
        super(props);
        registerClasses();
    }

    /**
     * This method is called when the class is instantiated. For now, all
     * Ontologies that should be known to Mulan are listed hard-coded here.
     *
     */
    private static void registerClasses() {
        de.renew.agent.${smallAppName}.ontology.${appName}Ontology.registerClasses();
    }
}